##

- Do password encryption/hashing in the application layer
- When signed in, the jwt token should have the user_id and the role
    - Do error handling using the exceptions in fastapi or something
